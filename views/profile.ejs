<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>User Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen p-10">
    <div class="max-w-2xl mx-auto bg-white p-8 rounded shadow-md">
        
        <div class="flex justify-between items-center border-b pb-4 mb-6">
            <h1 class="text-3xl font-bold text-gray-800">My Profile</h1>
            <a href="/" class="text-blue-500 hover:underline">Back to Home</a>
        </div>

        <div class="space-y-4">
            <div>
                <label class="block text-sm font-bold text-gray-500 uppercase">Username</label>
                <p class="text-lg text-gray-900"><%= user.username %></p>
            </div>
            
            <div>
                <label class="block text-sm font-bold text-gray-500 uppercase">Email</label>
                <p class="text-lg text-gray-900"><%= user.email %></p>
            </div>

            <div>
                <label class="block text-sm font-bold text-gray-500 uppercase">Bio</label>
                <p class="text-lg text-gray-900 italic">"<%= user.profile %>"</p>
            </div>
        </div>

        <div class="mt-8 pt-4 border-t flex justify-end">
            <a href="/logout" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600 transition">
                Logout
            </a>
        </div>

    </div>
</body>
</html>